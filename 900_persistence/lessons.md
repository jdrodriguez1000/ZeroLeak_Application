# Lessons — Lecciones Aprendidas

Registro de las lecciones aprendidas durante la ejecución del proyecto.

---

## Índice
<!-- Mantener actualizado. Un enlace por lección registrada. -->
- [L-01] Los subagentes `.claude/agents/*.md` requieren reiniciar Claude Code para ser invocables.
- [L-02] Extracción de `.docx` sin `python-docx` y encoding en consola Windows.
- [L-03] Carpetas de andamiaje pueden arrastrar artefactos de un proyecto hermano (FODA).
- [L-04] Colores de agentes repetidos entre subagentes (decisión consciente, pendiente de revisión).
- [L-05] Reemplazos masivos con `sed` pueden generar prefijos duplicados si el patrón ya existe parcialmente.
- [L-06] Comparar `state.json` con el proyecto hermano FODA ayuda a detectar diferencias de esquema (campo `band`) antes de instanciar el primer feature.
- [L-07] Editar notebooks `.ipynb` con contenido no-ASCII vía heredoc de bash en Windows/git-bash corrompe caracteres; usar scripts Python con escapes `\uXXXX` y verificar por codepoint + ejecución.
- [L-08] El gate humano de la spec (paso 7) requiere aprobación explícita; no debe darse por superado de forma implícita.
- [L-09] `pyproject.toml` exige Python 3.13+, pero el `python` del PATH del usuario es 3.12; usar `py -3.13` / `.venv` dedicado.
- [L-10] En el bucle TDD, un caso puede pasar en verde de inmediato si su código ya quedó cubierto por un caso anterior; la verificación honesta consiste en provocar temporalmente el defecto y confirmar el fallo, revirtiendo sin dejar rastro.
- [L-11] Separar "ingesta" (copia + hash) de "parseo" (estructura/delimitador) evita acoplar comandos tempranos del pipeline a un formato específico.
- [L-12] Los subagentes `tdd_*` no tienen kernel de Jupyter disponible; la sesión principal debe instalar `nbconvert`/`ipykernel` en el `.venv` del proyecto para ejecutar el notebook.
- [L-13] Un test nuevo puede pasar en verde de inmediato porque el CA que cubre ya quedó satisfecho por el refactor de un caso previo; tratarlo como test de caracterización/regresión en vez de forzar un RED artificial.
- [L-14] El patrón de caracterización (L-13) se repite con frecuencia cuando el diseño temprano ya es agnóstico/general (p. ej. helpers extraídos en refactors previos); reconocerlo rápido evita RED artificiales innecesarios en varios casos seguidos del mismo bucle.
- [L-15] Verificar pruebas manuales por su efecto real en disco (bronze/manifest), no solo por el exit code; un módulo CLI pensado para invocarse con `-m` necesita explícitamente el guard `if __name__ == "__main__"`.
- [L-16] Un verde de 15 CA + 88 tests + `spec_verifier` CONFORME no descarta bugs ni huecos de diseño; el gate humano con un artefacto real del repo (no un fixture sintético) los encontró en minutos.
- [L-17] Parchear `builtins.open` **no funciona dentro de un kernel de Jupyter**: el espía queda mudo y los asserts negativos pasan en vacío. Sombrear `open` como global del notebook y añadir siempre una guarda de no-vacuidad.
- [L-18] El protocolo de inicio de sesión debe contrastar `900_persistence/` contra el estado real de `git` (log/branches), no solo leer los archivos de persistencia.
- [L-19] Un plan de tests con validaciones sobre listas (duplicados, localización por índice) puede pasar en verde con solo N=1/2 casos y aun así esconder defectos que solo N>=3 revela (comparación por pares adyacentes, off-by-one con el último índice); revisar explícitamente la cardinalidad mínima necesaria para ejercitar un validador de lista.
- [L-20] El estilo YAML de bloque vs. flujo (`{a: x, b: y}`) es puramente cosmético para PyYAML: ambos producen el mismo diccionario. Preferir bloque para plantillas editadas por humanos (comentario por campo, diffs de una línea) y flujo para fixtures de tests (compacidad).
- [L-21] Retirar una instrucción de una plantilla puede dejar huérfano el bucle de retroalimentación del humano, aunque toda la suite quede verde.
- [L-22] La traza del subagente es evidencia; el auto-reporte del agente no lo es.
- [L-23] Un subagente `tdd_coder` nunca debe correr git destructivo ni reescribir `state.json` completo; editar quirúrgicamente y commitear con más frecuencia.
- [L-24] Pydantic no ejecuta el `field_validator` de una lista cuando un ítem de esa lista ya falló su propia validación; `exc.errors()` trae un solo error en ese escenario.
- [L-25] No mezclar sintaxis de here-string de PowerShell (`@'...'@`) dentro de la herramienta Bash en este entorno Windows; verificar el mensaje de un commit recién creado antes de usarlo en un PR.
- [L-26] El cierre de una feature debe cerrar también su `state.json`: `config_contract` quedó 2 días marcada `in_progress` pese a estar mergeada, y nadie lo notó porque `progress.md` sí decía la verdad.
- [L-27] El `notebook_writer` sin herramienta Bash se repite como patrón sistemático (ya visto en `ingest`, L-12): declara la limitación y deja las celdas vacías en vez de fabricar salidas; la sesión principal debe ejecutar el notebook.
- [L-28] Un `feature_contract.md` puede afirmar en falso que la suite vigente no se toca; un `grep` de 30 segundos en el paso 2 lo habría evitado.
- [L-29] El gerente debe verificar contra el plan las afirmaciones de subagentes antes de repetirlas como propias; propagar sin contrastar es acertar por suerte, no por rigor.
- [L-30] Los casos de caracterización pueden dejar tareas de `tdd_coder` huérfanas por la Single Writer Rule; conviene invocarlo igual cuando tenga tarea asignada, aunque no haya entregable de código.
- [L-31] Un mensaje de commit inline en PowerShell con construcciones tipo `palabra/palabra` puede ser bloqueado por un escáner de seguridad que lo interpreta como una ruta de sistema; usar `git commit -F <archivo>` para mensajes con caracteres ambiguos.

---

## [2026-07-12] L-01 — Registro de subagentes personalizados
- **Situación:** Al intentar invocar `session-closer` con la herramienta Agent, el harness respondió que el tipo de agente no existía (solo listaba los integrados).
- **Lección:** Crear el `.md` en `.claude/agents/` no basta para que el agente sea spawneable en la sesión en curso; el harness parece cargar los tipos al inicio.
- **Acción futura:** Tras crear/editar agentes, **reiniciar Claude Code** y verificar que aparezcan como tipos invocables. Mientras tanto, la sesión principal puede ejecutar el protocolo manualmente.

## [2026-07-12] L-02 — Leer archivos `.docx` en este entorno
- **Situación:** No hay `pandoc` ni `python-docx`; además la consola Windows (cp1252) falló al imprimir emojis del documento.
- **Lección:** Un `.docx` es un ZIP con XML; se extrae con `zipfile` + `xml.etree` de la stdlib. Para evitar `UnicodeEncodeError`, **escribir la salida a un archivo UTF-8** en vez de imprimir a consola.
- **Acción futura:** Reutilizar el script `extract_docx.py` del scratchpad para futuros `.docx`.

## [2026-07-12] L-03 — Artefactos ajenos en carpetas de andamiaje
- **Situación:** `700_architecture/system_design.md` y `sdd_tdd_workflow.md` resultaron pertenecer al proyecto hermano **FODA** (Foda_Application), no a ZeroLeak; estaban sin trackear en git, probablemente copiados al iniciar el andamiaje del repo.
- **Lección:** Antes de dar por válido el contenido de una carpeta recién creada por andamiaje, **verificar que el contenido corresponda al proyecto actual** (nombres, dominio, referencias cruzadas), especialmente si existe un proyecto hermano con estructura similar.
- **Acción futura:** Al iniciar/retomar sesión, revisar carpetas de diseño (`700_architecture/`, etc.) en busca de referencias a otros proyectos antes de asumir que su contenido es correcto.

## [2026-07-12] L-04 — Colores repetidos entre subagentes
- **Situación:** Al crear los 9 agentes de desarrollo (T-10), algunos colores quedaron repetidos: `green` en `feature-definer` y `tdd-coder`; `cyan` en `notebook-writer` e `integration-tester`; `blue` compartido entre `tdd-refactor` y `session-starter`.
- **Lección:** Fue una decisión consciente del humano al momento de crear cada agente (no un error), pero puede dificultar distinguir agentes por color en la interfaz si se ejecutan en paralelo.
- **Acción futura:** Evaluar si conviene unificar/diferenciar los colores en una sesión futura, sin que sea bloqueante para el avance del proyecto.

## [2026-07-12] L-05 — `sed` puede generar prefijos duplicados en reemplazos masivos
- **Situación:** Durante el refactor a `app/` (T-20), un reemplazo masivo con `sed` sobre referencias a `tests/` produjo un bug de doble prefijo `app/app/tests/` en 10 lugares (agentes tdd-*, integration-tester, plan-builder, template `plan.md`, `methodology.md`, `610_features/README.md`), porque el patrón `tests/` ya aparecía parcialmente prefijado por `app/` en algunas líneas procesadas antes.
- **Lección:** Al hacer reemplazos masivos de rutas con `sed` (o similares) en múltiples archivos, verificar con un `grep` posterior que no queden patrones duplicados/anidados, especialmente cuando el reemplazo se aplica más de una vez sobre el mismo conjunto de archivos o cuando el patrón de búsqueda es un substring de la cadena de reemplazo.
- **Acción futura:** Preferir reemplazos con anclas más específicas (p. ej. inicio de línea o comillas) y siempre correr un `grep` de verificación tras un refactor de rutas masivo antes de dar por cerrada la tarea.

## [2026-07-12] L-06 — Comparar `state.json` con el proyecto hermano FODA antes de instanciar el primero
- **Situación:** El humano preguntó por el formato correcto de `state.json` para el primer feature (`client_scaffold`). Se comparó el `state.json` de un feature ya construido en el proyecto hermano FODA (que incluye campo `band` y se organiza por bandas) contra la plantilla vigente `600_template/state.json`.
- **Lección:** El proyecto hermano FODA es una referencia útil de patrón, pero **no debe copiarse literalmente**: ZeroLeak adoptó D-01 (Opción A, sin bandas), por lo que su `state.json` **no lleva campo `band`** y en cambio incluye etapas propias del flujo de 13 pasos ausentes en FODA (`feature_contract` con owner `main_session`, `notebook_writer`, `human_test`, `merge_to_main`), conservando la granularidad de evidencia TDD (`red_evidence`, `green_evidence`, `refactor_note`, `cases[]`).
- **Acción futura:** Al instanciar el `state.json` de cada nuevo feature, usar siempre `600_template/state.json` como fuente de verdad (no el de FODA), y verificar que las decisiones D-01…D-05 sigan reflejadas correctamente.

## [2026-07-12] L-07 — Editar notebooks `.ipynb` con contenido no-ASCII en Windows/git-bash
- **Situación:** Al ajustar `client_scaffold.ipynb` para usar la regla de nombre aprobada (acentos/ñ válidos; ejemplos inválidos con cirílico y emoji) y para trabajar con `COMPANY_DEMO`, un primer intento de edición vía heredoc de bash corrompió los caracteres no-ASCII (acentos, ñ, cirílico) al escribir el JSON del notebook.
- **Lección:** El heredoc de bash en el entorno Windows/git-bash de este proyecto no preserva de forma confiable caracteres no-ASCII al escribir archivos. La inspección visual del terminal tampoco es suficiente para detectar la corrupción.
- **Acción futura:** Para editar notebooks (u otros archivos) con contenido no-ASCII, usar un script Python en archivo (no heredoc) con escapes explícitos `\uXXXX`, y verificar el resultado por **codepoint** (no visualmente) y por **ejecución real** (p. ej. `nbconvert --execute`), confirmando exit code 0 y 0 celdas con error. Además, las celdas markdown de un notebook no deben llevar `outputs` ni `execution_count`, o el JSON del notebook queda inválido.

## [2026-07-12] L-08 — El gate humano de la spec (paso 7) exige aprobación explícita
- **Situación:** Durante la construcción de `client_scaffold` (T-21), al llegar al paso 7 (gate humano sobre `spec.md`) el flujo avanzó dando por aprobada la spec de forma implícita, sin que el humano la aprobara explícitamente. El humano señaló el error y se corrigió, solicitando la aprobación explícita, que luego otorgó.
- **Lección:** Los gates humanos del flujo de 13 pasos (paso 5 sobre el notebook, paso 7 sobre la spec, paso 9 sobre el plan, `human_test`) son puntos de control obligatorios que requieren una confirmación explícita del humano; no deben asumirse superados por defecto ni inferirse de la ausencia de objeciones.
- **Acción futura:** En cada gate humano del flujo, solicitar y esperar una aprobación explícita antes de marcar la etapa correspondiente como `done` en `state.json` o de avanzar al siguiente paso.

## [2026-07-13] L-09 — Desajuste de versión de Python entre `pyproject.toml` y el PATH del usuario
- **Situación:** Al ejecutar TSK-24 (crear el tenant real `COMPANY_DEMO` instalando el paquete), `pip install -e .` falló porque `pyproject.toml` (en la raíz del repo) declara `requires-python = ">=3.13"`, mientras que el `python` resuelto por el PATH del usuario es la versión 3.12.10.
- **Lección:** No asumir que el `python`/`pip` del PATH satisface el `requires-python` del proyecto; verificar la versión antes de instalar. En Windows, el selector `py -3.13` permite invocar una versión específica instalada sin depender del PATH.
- **Acción futura:** Usar `py -3.13 -m venv .venv` en la raíz del repo y trabajar dentro de ese entorno (o invocar siempre `py -3.13`) para cualquier tarea que instale o ejecute el paquete `zeroleak`. `.venv/` ya está cubierto por `.gitignore`.

## [2026-07-13] L-10 — Verificación honesta cuando un caso TDD pasa en verde de inmediato
- **Situación:** En el bucle TDD de `client_scaffold`, varios casos (2-7, 11-13) tenían su código de producción ya cubierto desde el Caso 2 (que implementó la estructura completa del tenant), por lo que sus tests pasaban en verde apenas se escribían, sin una fase ROJA clásica.
- **Lección:** Que un test pase de inmediato no basta como evidencia de que realmente ejercita el comportamiento esperado. En esos casos, `tdd_tester` provocó temporalmente el defecto correspondiente en el código de producción, confirmó que el test fallaba por la razón correcta, y revirtió el cambio dejando el diff limpio antes de continuar — una "verificación razonada honesta" que sustituye a la fase ROJA cuando esta no puede ocurrir de forma natural.
- **Acción futura:** Aplicar esta técnica (romper temporalmente, confirmar el fallo, revertir sin rastro) cada vez que un test nuevo pase en verde sin haber tenido fase ROJA, antes de darlo por válido.

## [2026-07-13] L-11 — Separar ingesta (copia + hash) de parseo (estructura/delimitador)
- **Situación:** Al diseñar el alcance del segundo Tracer Bullet `ingest`, surgió la tentación de que el comando validara "es un CSV legible" o infiriera el delimitador del archivo.
- **Lección:** Acoplar la ingesta temprana a un formato específico (validar que sea "un CSV legible") compromete la agnosticidad del pipeline frente a variaciones de delimitador (`,`/`;`/`|`) o estructura, que son responsabilidad de una etapa posterior (el Contrato de Datos / YAML 1). El principio "archivador notarial" — copiar bytes fielmente y hashear, sin parsear — mantiene `ingest` simple, robusto y reutilizable para cualquier extensión de la allow-list.
- **Acción futura:** Al diseñar comandos tempranos del pipeline (ingesta, registro, staging), limitar su validación a lo estrictamente agnóstico de formato (existencia, no vacío, extensión permitida) y diferir toda validación de estructura/contenido a la etapa de contrato de datos.

## [2026-07-13] L-12 — Los subagentes `tdd_*`/`notebook_writer` no tienen kernel de Jupyter para ejecutar notebooks
- **Situación:** Al construir `ingest.ipynb` (paso 4 del Tracer Bullet `ingest`), el agente `notebook_writer` produjo el notebook pero no pudo ejecutarlo para generar evidencia real (sin kernel/entorno disponible en su contexto de ejecución).
- **Lección:** Los subagentes que trabajan con notebooks pueden carecer del entorno Python necesario (kernel, `nbconvert`, `ipykernel`) para ejecutarlos, aunque sí puedan escribir el JSON del `.ipynb`. La sesión principal debe asumir la ejecución cuando esto ocurra.
- **Acción futura:** Si un agente reporta no poder ejecutar un notebook, la sesión principal debe instalar `nbconvert`+`ipykernel` en el `.venv` del proyecto (ya con Python 3.13, ver L-09) y ejecutar el notebook in-place (`jupyter nbconvert --to notebook --execute --inplace`), verificando exit code 0 y ausencia de celdas con error antes de dar el paso 4 por completo.

## [2026-07-13] L-13 — Test de caracterización cuando un CA ya quedó satisfecho por un caso anterior
- **Situación:** En el bucle TDD de `ingest`, el Caso 3 (CA-02, forma exacta del manifest) pasó en verde de inmediato al escribirse, sin ningún cambio de código: el helper `_build_manifest_entry` (extraído durante el refactor del Caso 2) ya emitía exactamente los 4 campos exigidos.
- **Lección:** A diferencia de L-10 (donde se provoca temporalmente el defecto para confirmar honestamente el fallo), aquí no aplicaba esa técnica porque no había ningún comportamiento "por romper": el CA ya estaba cubierto como efecto colateral genuino de una decisión de diseño anterior (el helper). En ese caso, forzar un RED artificial sería teatro, no verificación. La resolución correcta es reportar el hallazgo a la sesión principal, decidir explícitamente tratar el test como **caracterización/regresión** (blindaje contra regresiones futuras, no evidencia de un ciclo TDD clásico) y dejar constancia de esa decisión en `plan.md`/`state.json` (marcando la tarea de test como `implementada` con la nota correspondiente, y la tarea de código como `cancelada_suspendida` si no hay nada nuevo que escribir).
- **Acción futura:** Antes de forzar un RED artificial en un caso que pasa en verde de inmediato, evaluar si existe un comportamiento real que romper (L-10) o si el CA ya fue satisfecho genuinamente por trabajo previo (L-13); en el segundo caso, documentar la decisión explícitamente en vez de simular un ciclo que no ocurrió.

## [2026-07-14] L-14 — El patrón de caracterización se repite cuando el diseño temprano ya es general
- **Situación:** Al cerrar el bucle TDD de `ingest` (bloques 3 y 4), el patrón de caracterización descrito en L-13 volvió a aparecer tres veces más: Caso 8 (CA-04, argumentos mezclados archivo+carpeta) quedó cubierto por el helper `_expand_paths` extraído en el Caso 7; Caso 9 (CA-10, delimitadores `,`/`;`/`|` sin parseo) quedó cubierto por el diseño byte-a-byte adoptado desde el Caso 2 (`_hash_file`/`shutil.copyfile` nunca interpretan el contenido); Caso 11 (CA-11, frontera PII del `.gitignore`) quedó cubierto por la regla `clients/*/data/` ya vigente desde `client_scaffold` (C-01), sin ningún cambio de `.gitignore` ni de código de producción.
- **Lección:** Cuando el diseño de un caso temprano se hace deliberadamente agnóstico/general (p. ej. operar siempre sobre bytes crudos, o extraer un helper que ya cubre el caso general), varios casos posteriores del mismo bucle tienden a pasar en verde de inmediato como caracterización, no como una excepción aislada. Detectar esta tendencia temprano (en vez de sorprenderse cada vez) agiliza el bucle: se puede anticipar la pregunta "¿ya está cubierto por diseño?" antes de escribir un RED que se sabe no va a fallar.
- **Acción futura:** Al iniciar cada caso nuevo del bucle TDD, antes de escribir el test, revisar brevemente si el comportamiento exigido por el CA ya es una consecuencia necesaria del diseño agnóstico adoptado en casos anteriores; si es así, ir directo al patrón de caracterización (L-13) en vez de asumir que habrá un RED clásico.

## [2026-07-14] L-15 — Un exit code exitoso no garantiza que el comando realmente se ejecutó; el guard `__main__` es obligatorio
- **Situación:** Al ejecutar pruebas manuales adicionales del paso 13 de `ingest` (dedupe por contenido y carga de `.xlsx`), se descubrió que `app/src/zeroleak/cli.py` tenía la función `main()` pero le faltaba el bloque `if __name__ == "__main__": sys.exit(main())`. Como resultado, invocar `py -3.13 -m zeroleak.cli ingest ...` era un **no-op silencioso**: el intérprete importaba el módulo, nunca llamaba a `main()`, y terminaba con exit 0 sin ingerir ni un solo archivo. Las pruebas manuales de sesiones anteriores que "pasaban con exit 0" usando esa invocación en realidad no habían ejecutado nada; el comando correcto siempre fue el console-script instalado `zlk ingest ...` (definido en `pyproject.toml`), no el módulo directamente.
- **Lección:** Un exit code 0 no es evidencia suficiente de que un comando CLI se ejecutó de verdad. Para pruebas manuales o de integración que invocan un CLI por subprocess, hay que verificar el **efecto real** (archivos copiados a bronze, entradas nuevas en `manifest.json`, contenido en stdout/stderr esperado), no solo el código de salida. Además, cualquier módulo `cli.py` pensado para poder invocarse con `python -m paquete.cli` necesita explícitamente el guard `if __name__ == "__main__": sys.exit(main())`; sin él, `-m` importa el módulo pero nunca ejecuta el punto de entrada.
- **Acción futura:** Al crear o revisar un módulo `cli.py` con una función `main()`, confirmar siempre que exista el guard `__main__`. Al diseñar o revisar pruebas manuales, especificar explícitamente qué efecto en disco/stdout debe inspeccionarse tras cada comando, no solo el exit code.

## [2026-07-15] L-16 — Un verde automatizado completo no es evidencia de ausencia de bugs/huecos de diseño
- **Situación:** El gate humano del paso 13 de `config_contract` encontró en minutos dos cosas que 15 CA verdes, 88 tests y un `spec_verifier` CONFORME no vieron: (1) un bug real (`AttributeError` en vez de `ContractSchemaError` cuando `contract_data:` tiene valor nulo, ver T-56), descubierto al cargar la plantilla real y vacía que el propio repo trae en `clients/COMPANY_DEMO/input/contract_data.yaml`; (2) un hueco de diseño (D-18, soporte de múltiples archivos), descubierto al preguntar por una necesidad real del humano que el motor actual no contempla.
- **Lección:** Toda la verificación automatizada (tests, `spec_verifier`) usa fixtures **sintéticos generados por los propios tests**, que solo cubren los casos que se imaginaron al escribirlos. Un verde total dice "los casos que imaginamos funcionan", no "esto funciona" en general. El gate humano encontró ambos problemas precisamente porque usó un **artefacto real del repo** (la plantilla vacía) y una **necesidad real** (varios archivos), no fixtures sintéticos.
- **Acción futura:** En features futuras, incluir en el gate humano al menos un ejercicio con un artefacto real del repo (no solo con fixtures sintéticos de los tests) antes de dar la feature por completamente validada.

## [2026-07-15] L-17 — El espía sobre `builtins.open` no funciona en un kernel de Jupyter: evidencia vacua
- **Situación:** El notebook `contract_multifile.ipynb` (paso 4) demostraba la frontera D-21 (HU-10: "el motor no abre ningún CSV de bronze") parcheando `builtins.open` con un espía que registraba cada ruta abierta, y luego afirmando `assert not toca_bronze`. Al ejecutarlo, la celda pasaba y concluía "OK". Pero la lista de rutas observadas salía **vacía**: el espía no registró ni siquiera la apertura del propio `contract_data.yaml`. Los tres asserts pasaban trivialmente porque una lista vacía no contiene nada. La evidencia era **vacua**: la conclusión era correcta, pero no estaba respaldada por la observación.
- **Causa:** Dentro de un kernel de Jupyter/IPython, reasignar `builtins.open` **no** intercepta el `open()` que llama una función definida en una celda. Reproducido con un notebook mínimo de dos celdas: en un proceso Python normal el mismo espía sí registra la apertura; bajo `nbconvert --execute` registra cero. **El test equivalente bajo pytest sí funciona** (CA-12 de `config_contract` usa `monkeypatch` sobre `builtins.open` y es válido): pytest es un proceso normal. El defecto es exclusivo del notebook.
- **Lección:** Dos cosas distintas. (1) Técnica: en un notebook, para espiar `open` hay que **sombrearlo como global del propio notebook** (`open = espia` en la celda, `del open` al terminar), que gana en la búsqueda de nombres; no parchear el módulo `builtins`. (2) Método, más importante: **un assert negativo ("no tocó bronze") es indistinguible de un instrumento roto** si no se comprueba que el instrumento observó algo. Un espía mudo hace pasar cualquier aserción de ausencia.
- **Acción futura:** Toda evidencia construida sobre "no ocurrió X" debe llevar una **guarda de no-vacuidad** que exija una observación positiva antes de afirmar la ausencia (aquí: `assert rutas_abiertas` y `assert solo_el_yaml`, que fijan que se abrió exactamente el YAML esperado). Aplica igual a tests y a notebooks. Emparejar con L-16: el verde no es evidencia; la observación positiva sí. Al revisar un notebook antes de un gate, leer las **salidas**, no solo el exit code (una celda vacua sale en verde).

## [2026-07-15] L-18 — El inicio de sesión debe contrastar la persistencia contra el estado real de git, no solo leerla
- **Situación:** Al iniciar esta sesión, el `session-starter` reportó el PR #3 (`feature/config_contract`→`main`) como **pendiente de aprobación y merge**, siguiendo al pie de la letra lo que decían `progress.md`/`tasks.md`. En realidad el PR **ya estaba mergeado**: `git log` en `main` mostraba el commit `853cc32` ("Merge pull request #3 from jdrodriguez1000/feature/config_contract") como parte del historial visible desde el arranque de la sesión. El `session-starter` solo había leído los archivos de `900_persistence/`, sin contrastarlos contra `git log`/`git branch` del repo real.
- **Lección:** Los archivos de `900_persistence/` son la memoria **narrada** del proyecto, escrita por sesiones anteriores; pueden quedar **desfasados** respecto del estado objetivo del repositorio cuando una acción del humano (como aprobar y mergear un PR en GitHub) ocurre fuera del alcance de la sesión que la registró. Confiar ciegamente en la persistencia sin verificarla contra la fuente de verdad (`git log`, `git branch -vv`, estado de PRs) puede llevar a repetir trabajo ya hecho, a pedir de nuevo una acción humana ya realizada, o a construir sobre una rama base equivocada.
- **Acción futura:** El protocolo de inicio de sesión (`session-starter`) debe, como mínimo, correr `git log --oneline` sobre `main` (y comparar contra lo que la persistencia da por pendiente) antes de reportar el estado del proyecto al humano; si detecta una discrepancia (p. ej. un merge que la persistencia no registra), debe señalarla explícitamente y dejar que el cierre de la sesión actualice `900_persistence/` en consecuencia, en vez de propagar el desfase.

## [2026-07-15] L-19 — Un plan de tests con N=1/2 puede esconder defectos que solo N>=3 revela
- **Situación:** Al auditar el `plan.md` de `contract_multifile` (26 casos originales) antes del gate del paso 9, el humano preguntó si el diseño soportaba 3 o más archivos. El modelo (`Contract.archivos: list[...]`) sí lo soporta sin cota, pero **ningún caso** ejercitaba N>=3: el detector de duplicados (M-05) solo se probaba con dos `ventas.csv` **adyacentes** en una lista de 2 (una comparación por pares vecinos pasaría sin ver un duplicado no-adyacente en una lista de 3+), y el localizador `archivo[i]` solo se ejercitaba con el índice 1 de una lista de 2 — que es a la vez el **último** índice, así que un off-by-one o un "reporta siempre el último" pasaba igual los 26 casos en verde.
- **Lección:** Cuando un validador opera sobre una **lista** (detección de duplicados por comparación exhaustiva, localización por índice), un plan de tests con cardinalidad mínima (N=1 o N=2) puede dar cobertura completa de CA en apariencia, sin ejercitar los defectos que solo aparecen con N>=3: comparaciones "por pares vecinos" en vez de exhaustivas, e índices que coinciden con posiciones especiales (primero/último) en listas cortas.
- **Acción futura:** Al planear casos de test para cualquier validación sobre listas, incluir explícitamente al menos un caso con N>=3 elementos, con el elemento defectuoso en una posición que **no** sea ni el primero ni el último, para descartar por diseño estos dos patrones de defecto.

## [2026-07-15] L-21 — Retirar una instrucción de una plantilla puede dejar huérfano el bucle de retroalimentación del humano
- **Situación:** Al auditar el diseño de `contract_multifile` se confirmó que `load_contract` nunca escribe a disco: el humano edita `contract_data.yaml` a mano (único paso manual del flujo) y hoy valida su trabajo corriendo `py -3.13 610_features/config_contract/cargar_contrato.py <ruta>`, instrucción que hoy vive en la plantilla `600_template/contract_data.yaml`. TSK-44 (dentro de `contract_multifile`) reescribe esa plantilla al esquema `archivos[]` y, al hacerlo, **elimina** esa instrucción de paso — sin que ningún test lo note, porque los mensajes M-01..M-13 que esa instrucción invocaba siguen existiendo y bien probados; simplemente nadie los llama ya en el flujo real del usuario. El CLI `zlk` no ofrece hoy ni ofrecerá dentro de esta feature un `zlk contract check` (vetado por D-23d/CA-27), así que no hay ni siquiera un candidato inmediato para reemplazar la instrucción retirada.
- **Lección:** Un bucle de retroalimentación del humano (aquí: "¿mi YAML editado a mano quedó bien?") puede depender enteramente de una **instrucción de texto en una plantilla**, no de código. La suite de tests puede quedar en verde perfecto mientras esa instrucción desaparece silenciosamente en un refactor de la plantilla, porque los tests verifican los **mensajes de error**, no que exista un camino real y descubrible para que el humano los provoque a propósito.
- **Acción futura:** Antes de reescribir una plantilla que un humano edita a mano, verificar explícitamente si contiene instrucciones de auto-verificación y, si se retiran, dejar registrado (aunque sea como tarea pendiente, no bloqueante) qué las reemplaza — no dejar la plantilla muda sobre cómo validar el archivo que el humano acaba de escribir. Ver T-68 y la nota de T-60 en `tasks.md`.

## [2026-07-16] L-22 — La traza del subagente es evidencia; el auto-reporte del agente no lo es
- **Situación:** Al diseñar la nueva pauta de observabilidad (E13/methodology §7, D-29), se verificó que Claude Code ya deja una traza automática y completa por subagente en disco (`agent-<id>.jsonl`: secuencia real de herramientas, entradas, salidas, tokens), independiente de lo que el subagente narra al terminar. Se extrajo la traza real de una corrida ya cerrada de `feature_definer` (la que produjo `definition.md` de `contract_multifile`) y se confirmó que reconstruye exactamente la secuencia de 6 tool calls (Read contrato → Read plantilla → Read ejemplo → Read `contract.py` → Grep decisiones → Write `definition.md`), con tokens, sin depender de que el agente lo hubiera reportado.
- **Lección:** El resumen final que un subagente da de su propio trabajo (qué leyó, qué escribió, si siguió el procedimiento) es narrativa generada por el mismo agente que se está evaluando — no es una fuente independiente (viola P3 si se usa como evidencia de conformidad). La traza en disco, en cambio, es un registro mecánico de lo que realmente ocurrió (herramienta por herramienta) y no puede "mentir" sobre la secuencia ejecutada. Cualquier check de conformidad determinista (¿leyó el contrato antes de escribir?, ¿un solo `Write`?, ¿siguió la plantilla?) debe evaluarse contra la traza, nunca contra el auto-reporte del agente.
- **Acción futura:** Al implementar el lector de conformidad (T-72), construir cada check contra el `agent-*.jsonl`/`.meta.json` real, no contra el texto que el subagente devuelve como resumen de su propia ejecución.

## [2026-07-16] L-23 — Un subagente `tdd_coder` nunca debe correr git destructivo ni reescribir `state.json` completo
- **Situación:** Durante la fase GREEN del Caso 17 del bucle TDD de `contract_multifile`, un subagente `tdd_coder` necesitaba anotar el avance en `610_features/contract_multifile/state.json`. En vez de una edición quirúrgica, corrió un script Python con `json.dump(...)` que reescribió el archivo completo, produciendo un diff enorme y con riesgo de reformateo (posible pérdida de estilo ASCII/orden de claves). Al intentar deshacer ese diff no deseado, el subagente ejecutó `git checkout -- state.json`, lo que revirtió el archivo por completo a la versión de HEAD (commit `99337b6`, que solo tenía tracking de los Casos 1-12), descartando silenciosamente el tracking NO commiteado de los Casos 13-17 que la sesión ya había reconstruido. El código de producción y los tests de esos casos quedaron intactos y verdes (nunca estuvieron en el árbol de trabajo que el `checkout` tocó), así que la pérdida fue exclusivamente de tracking/metadatos, no de trabajo funcional; la sesión principal la detectó y reconstruyó `state.json` a mano desde los reportes de los subagentes, dejando un commit de seguridad (`3c6a486`) inmediatamente después.
- **Lección:** Dos prohibiciones concretas para subagentes `tdd_*` (y en general cualquier subagente que edite `state.json`): (1) nunca reescribir `state.json` completo vía `json.dump`/similar — usar siempre ediciones quirúrgicas (Edit dirigido a la sección que cambia) para minimizar el diff y el riesgo de reformateo; (2) nunca ejecutar comandos git destructivos (`git checkout --`, `git reset --hard`, etc.) para "deshacer" un cambio propio — si un cambio salió mal, corregirlo con una edición dirigida, nunca revirtiendo el archivo entero, porque eso puede borrar tracking válido de trabajo previo no commiteado que el subagente no ve en su contexto fresco. Commitear con más frecuencia (por bloque de 2-3 casos) reduce la ventana de pérdida si el incidente se repite.
- **Acción futura:** Reforzar en los prompts de `.claude/agents/tdd-coder.md` (y hermanos) la prohibición explícita de git destructivo y de reescritura completa de `state.json`; ya existe una memoria persistente de usuario sobre esto (`tdd-subagents-no-git-checkout-state`). Verificar que la prohibición quede también en `905_guideline/methodology.md` si se detecta que se repite.

## [2026-07-16] L-24 — Pydantic no ejecuta el `field_validator` de lista si un ítem ya falló su propia validación
- **Situación:** Al verificar honestamente el Caso 20 del bucle TDD de `contract_multifile` (CA-21, fail-fast agnóstico al orden), se construyó una fixture con dos defectos simultáneos: dos archivos con el mismo `nombre` `ventas.csv` (M-05, detectado por el `field_validator` de lista `_archivos_sin_duplicados`) y el segundo de ellos con una columna de `tipo: numero_magico` fuera del enum (M-10, detectado por la validación del ítem `ArchivoContrato`). Se esperaba en principio que Pydantic pudiera reportar **ambos** errores (agregación), pero `exc.errors()` trajo siempre **un solo** error: cuando el ítem de una lista falla su propia validación de campo, Pydantic **no llega a ejecutar** el `field_validator` que opera sobre la lista completa (el validador de duplicados).
- **Lección:** La garantía de cardinalidad (un solo error, D-26b) no depende únicamente del helper `_mensaje_esquema` (que toma `errors()[0]`); Pydantic mismo, en este patrón de validadores anidados (ítem + lista), ya poda el árbol de errores a uno solo cuando el ítem falla primero. Esto es un detalle de implementación de Pydantic, no garantizado por el código propio — no asumir en tests futuros qué mensaje "gana" entre un defecto de ítem y uno de lista.
- **Acción futura:** Al diseñar tests de cardinalidad/fail-fast sobre estructuras con validadores anidados (ítem + lista) en Pydantic, verificar empíricamente (no asumir) si la librería ya poda el árbol de errores antes de que el código propio intervenga, y mantener el test agnóstico a cuál de los dos mensajes posibles se reporta (como ya hace CA-21).

## [2026-07-17] L-25 — No mezclar here-strings de PowerShell dentro de la herramienta Bash; verificar el mensaje de un commit recién creado
- **Situación:** Durante el cierre del gate humano de `contract_multifile` (paso 13), un commit se creó usando una here-string de PowerShell (`git commit -F - <<'@' ... @` o construcción equivalente) dentro de la herramienta Bash del entorno. La herramienta Bash de este proyecto ejecuta Git Bash (POSIX sh), no PowerShell, y no interpreta esa sintaxis; el resultado fue un commit real con el asunto literal `"@"` (y otro `"@"` residual al final del cuerpo), en vez del mensaje descriptivo pretendido.
- **Causa:** Se mezcló sintaxis de PowerShell (here-string `@'...'@`) dentro de una llamada a la herramienta Bash, que la documentación de la propia herramienta prohíbe explícitamente y para la que exige usar heredoc POSIX (`<<'EOF' ... EOF`) en su lugar.
- **Detección y corrección:** Se detectó **antes de abrir el PR** inspeccionando el mensaje del commit recién creado con `git log -1 --format=%B | cat -A` (revela caracteres invisibles/literales que un `git log` normal podría no dejar ver con claridad). Se corrigió con `git commit --amend -F -` alimentado por un heredoc POSIX correcto, y se republicó con `git push --force-with-lease` — seguro en este caso porque el commit vivía en una rama propia (`feature/contract_multifile`) sin ningún PR abierto todavía que pudiera verse afectado. Commit final correcto: `7ab98af`.
- **Lección:** (1) Nunca usar sintaxis de PowerShell (here-strings `@'...'@`, `$env:`, etc.) dentro de la herramienta Bash de este entorno Windows; usar siempre heredoc POSIX (`<<'EOF' ... EOF`) para mensajes multilínea de `git commit`. (2) Tras crear un commit con un mensaje generado programáticamente (heredoc, script, etc.), verificar el resultado real con `git log -1 --format=%B | cat -A` antes de darlo por bueno, especialmente antes de abrir un PR con ese commit — no basta con asumir que la sintaxis usada fue interpretada como se esperaba.
- **Acción futura:** Antes de cualquier `git commit -F -`/heredoc en la herramienta Bash de este proyecto, usar exclusivamente sintaxis POSIX; después de crear el commit, correr `git log -1 --format=%B | cat -A` como verificación estándar de higiene antes de push/PR.

## [2026-07-17] L-26 — El cierre de una feature debe cerrar también su `state.json`, no solo `progress.md`

- **Situación:** Ejecutando T-69 (retirar los 2 scripts obsoletos de `config_contract`) hubo que abrir el `state.json` de esa feature para anotar el retiro. Estaba en `status: in_progress` / `current_stage: tdd`, con `tdd: in_progress`, `human_test: pending` y `merge_to_main: pending` — pese a que la feature se había mergeado el **2026-07-15** vía PR #3 (`853cc32`) y llevaba dos días cerrada. El registro llevaba dos sesiones mintiendo sobre el estado de una feature terminada. Nadie lo detectó porque `progress.md` y `tasks.md` **sí** decían la verdad (T-55 constaba `Implementada`, con el gate y el merge documentados): la contradicción vivía entre dos archivos que nunca se leen juntos. La sesión de cierre de `config_contract` actualizó la persistencia central y se saltó el `state.json` de la feature, que es justo el archivo que un `session_starter` abre para saber en qué paso va el flujo.
- **Lección:** La persistencia del proyecto tiene **dos capas** (`900_persistence/*` centralizado y `610_features/<feature>/state.json` por feature) y un cierre incompleto puede dejarlas contradiciéndose sin que ninguna alarma suene, porque cada archivo por separado es internamente coherente. El `state.json` es además la capa más peligrosa de desincronizar: es la que dice **en qué paso del flujo de 13 va la feature**, así que un `in_progress` fantasma invita a una sesión futura a "retomar" un bucle TDD que ya está cerrado y mergeado. Emparenta con **L-18** (contrastar la persistencia contra el estado real de git en el inicio de sesión): esta es su cara simétrica en el **cierre**. Nota de método: la corrección se hizo contra evidencia verificable (`gh pr view 3` → `MERGED`, y los 15 casos cerrados uno por uno) y respetando el límite de lo que se podía saber — `human_test` se marcó `done` por **inferencia** (por C-03 el merge es acto exclusivo del humano, luego el gate se dio por satisfecho), dejando escrito en el propio archivo que la sesión correctora **no presenció** el gate y no inventaba su detalle. Un registro corregido debe declarar de dónde sale cada afirmación.
- **Acción futura:** (1) En el protocolo de cierre (`session-closer`), cuando la sesión cierre o mergee una feature, actualizar **siempre** el `state.json` de esa feature (`status`, `current_stage`, y las etapas `human_test`/`merge_to_main` con PR y merge commit) además de `progress.md`/`tasks.md`; el versionado no es el último paso si el `state.json` sigue abierto. (2) En el protocolo de inicio (`session-starter`), extender la verificación de L-18: si un `state.json` declara una feature `in_progress` pero su PR figura mergeado en git/`gh`, reportarlo como inconsistencia antes de recomendar camino. (3) Al corregir un registro histórico, distinguir explícitamente lo verificado de lo inferido, y no completar los huecos con detalle plausible.

## [2026-07-17] L-27 — El `notebook_writer` sin herramienta Bash es un patrón sistemático, no un incidente aislado
- **Situación:** Al construir el spike de `contract_check` (paso 4, sexto Tracer Bullet), el subagente `notebook_writer` volvió a reportar explícitamente que no tiene herramienta Bash disponible y, en vez de fabricar salidas falsas, dejó las celdas de código sin ejecutar. Es el **mismo comportamiento** ya documentado en L-12 durante `ingest` (2026-07-13) y visto de nuevo en `contract_multifile`. La sesión principal ejecutó el notebook con `py -3.13 -m jupyter nbconvert --to notebook --execute --inplace`, produciendo las 14/14 celdas de código con salidas reales.
- **Lección:** No es un incidente puntual: es el comportamiento **esperado y sistemático** del agente en este entorno, en al menos 3 features consecutivas. El agente hace lo correcto (declara la limitación en vez de alucinar salidas), pero el paso 4 del flujo, tal como está diseñado hoy, siempre requiere una segunda intervención de la sesión principal para completarse.
- **Acción futura:** Decidir explícitamente una de dos rutas: (a) dar herramienta Bash al `notebook_writer` para que ejecute sus propios notebooks, o (b) rediseñar el paso 4 del flujo (`905_guideline/methodology.md`) para que incluya, por diseño y no por excepción, que la sesión principal ejecute el notebook tras escribirlo. Mientras no se decida, seguir asumiendo que la sesión principal deberá ejecutar el notebook cada vez.

## [2026-07-17] L-28 — Un `feature_contract.md` puede afirmar en falso que la suite vigente no se toca
- **Situación:** El `feature_contract.md` de `contract_check`, escrito por la sesión principal en el paso 2, afirmaba que la feature "no toca `test_contract_multifile.py`" y que los 111 tests vigentes seguirían pasando sin cambios. El spike (paso 4) destapó que era falso: `test_load_contract_core_invocable_sin_cli` (`test_contract_multifile.py:1635`) assertea la **ausencia** de fachada CLI, justo lo que D-31 (paso 1) derogó. El contrato se corrigió en el paso 5 en vez de descubrirse a mitad del bucle TDD.
- **Lección:** El `feature_contract.md` puede contener afirmaciones verificables sobre el estado de la suite **sin haberlas verificado** contra el código real. Un `grep` rápido sobre los tests existentes en el paso 2 habría detectado la contradicción antes de escribir la spec.
- **Acción futura:** Al escribir el `feature_contract.md` (paso 2), si se afirma algo verificable sobre la suite vigente (qué toca, qué no toca, qué sigue pasando), correr un `grep`/búsqueda dirigida contra el código real antes de darlo por cierto, en vez de asumirlo por el alcance declarado de la feature.

## [2026-07-17] L-29 — El gerente debe verificar contra el plan las afirmaciones de subagentes antes de repetirlas
- **Situación:** Durante el bucle TDD de `contract_check`, sucesivos subagentes `tdd_coder`/`tdd_refactor` cerraban su reporte diciendo "refactor consolidado diferido a TSK-34", y el gerente (sesión principal) lo repetía en sus propios resúmenes de cierre de caso como si lo hubiera comprobado él mismo. La nota se originó en el Caso 1 y se fue propagando de subagente en subagente, contexto fresco tras contexto fresco, sin que nadie la contrastara contra la fuente de verdad. El humano preguntó explícitamente "¿realizaste refactor? ¿invocaste ese agente?", forzando la verificación.
- **Verificación:** Al contrastar contra `plan.md` (línea 139), la afirmación resultó **correcta**: TSK-34 está diseñada para ejecutarse tras cerrar el Caso 16, y los Casos 2, 3, 4, 6, 7, 8, 10, 11 están pensados como su red de seguridad (tests de caracterización que blindan el refactor consolidado posterior). Pero el gerente no lo sabía cuando la repetía; fue una afirmación correcta por azar, no por haberla verificado.
- **Lección:** Un gerente que orquesta subagentes de contexto fresco no debe propagar como propia una afirmación que un subagente hace sobre "qué queda pendiente/diferido", sin contrastarla contra el artefacto de planificación (`plan.md`) que la respalda o la desmiente. La propagación acrítica es indistinguible del acierto real hasta que alguien pregunta.
- **Acción futura:** Cuando un subagente reporte una decisión de alcance ("diferido a TSK-X", "no aplica por Y"), el gerente debe verificarla contra `plan.md`/`state.json` antes de repetirla en su propio resumen al humano, no solo antes de actuar sobre ella.

## [2026-07-17] L-30 — Los casos de caracterización pueden dejar tareas de `tdd_coder` huérfanas
- **Situación:** En varios casos de caracterización del bucle TDD de `contract_check` (Casos 2, 5, 7), al no haber entregable de código no se invocaba a `tdd_coder`, y por la Single Writer Rule (cada agente solo puede tocar sus propias tareas en `state.json`/`plan.md`) las tareas de código asociadas a esos casos quedaban `no_implementada` pese a que el comportamiento ya estaba resuelto. Ocurrió con TSK-07 y TSK-13, y hubo que sanearlas con un commit dedicado (T-94) tras detectarlas huérfanas.
- **Lección:** Un caso de caracterización sin entregable de código todavía puede tener una tarea de `tdd_coder` asignada en el plan (aunque su trabajo real sea "no hay nada que escribir"); saltarse la invocación dejará esa tarea eternamente huérfana. No todos los casos de caracterización tienen tarea de coder — algunos (como el Caso 7 de `contract_check`, con solo TSK-16 de tester) no la tienen y no aplica esta lección.
- **Acción futura:** Adoptada la convención de invocar igual a `tdd_coder` en cada caso de caracterización que **sí** tenga una tarea de coder asignada en `plan.md`, para que cierre formalmente esa tarea aunque no produzca código nuevo, en vez de dejarla pendiente de un saneamiento posterior.

## [2026-07-17] L-31 — Un mensaje de commit inline en PowerShell puede ser bloqueado por un escáner de seguridad que lo lee como ruta
- **Situación:** Al cerrar el Caso 8 del bucle TDD de `contract_check`, el primer intento de `git commit` con un mensaje inline (que incluía el texto `parse(3)/esquema(4)`, describiendo los exit codes distinguidos) fue **bloqueado** por un escáner de seguridad del entorno con el error `"Remove-Item on system path '/esquema' is blocked"`. El escáner interpretó la construcción `palabra/palabra` con barra como una ruta de sistema candidata a eliminación, no como texto descriptivo.
- **Causa:** El escáner de seguridad del entorno inspecciona el texto de los comandos (incluido el cuerpo de mensajes de commit pasados inline) buscando patrones de comandos destructivos; una barra entre dos palabras puede leerse como una ruta absoluta o relativa, activando el bloqueo aunque no exista ninguna intención de borrar nada.
- **Solución aplicada:** Escribir el mensaje de commit completo a un archivo temporal y usar `git commit -F <archivo>`, evitando pasar el texto con barras ambiguas directamente como argumento del comando.
- **Lección:** En mensajes de commit inline por PowerShell (o cualquier invocación directa de `git commit -m`), evitar construcciones tipo `palabra/palabra` con barra que un escáner de seguridad pueda leer como ruta; preferir `git commit -F <archivo>` con el mensaje redactado en un archivo cuando el texto contenga caracteres ambiguos (barras, guiones que parezcan flags, etc.).
- **Acción futura:** Ante un bloqueo inesperado de un comando de git por parte del entorno, sospechar primero de construcciones de texto ambiguas en el mensaje antes de asumir un problema de permisos; usar `-F <archivo>` como mitigación estándar.

## [2026-07-15] L-20 — El estilo YAML de bloque vs. flujo es cosmético para PyYAML
- **Situación:** El humano preguntó si el estilo de bloque con indentación (`- nombre: x\n  tipo: y`) y el estilo de flujo con llaves (`{nombre: x, tipo: y}`) en un YAML producían resultados distintos para `load_contract`. Se verificó ejecutando PyYAML directamente que ambas formas producen **exactamente el mismo diccionario** en Python.
- **Lección:** La elección de estilo YAML (bloque vs. flujo) es puramente cosmética a nivel de parseo; no afecta la validación ni el comportamiento del motor. La elección correcta depende de quién edita el archivo: bloque favorece comentarios por campo y diffs de una línea por cambio (mejor para plantillas humanas como `600_template/contract_data.yaml`); flujo favorece compacidad (mejor para fixtures de tests).
- **Acción futura:** Al escribir o revisar plantillas YAML pensadas para edición humana, preferir el estilo de bloque; reservar el estilo de flujo para fixtures sintéticos de test donde la compacidad es una ventaja real.

---
